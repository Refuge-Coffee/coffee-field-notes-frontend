<h2>Notes</h2>

<div class="container">
  {{#each model as |note i|}}
    <div class="box box{{i}}">
      <h4>{{moment-format note.dateRecorded 'MM-DD-YYYY' 'MM-DD-YYYY'}} - {{note.timeOfDay}}</h4>
    </div>
  {{/each}}
</div>

{{outlet}}
