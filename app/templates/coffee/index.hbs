<div class="introduction-container">
  <div class="box box">
    <div class="box-content">
      <h1 class="headline centered">Coffee</h1>
    </div>
  </div>
</div>

<div class="container">
  {{#each model as |coffee i|}}
    <div class="coffee box box{{i}}">
      {{#coffee-box-content coffee=coffee}}{{/coffee-box-content}}
    </div>
  {{/each}}
</div>

{{outlet}}
